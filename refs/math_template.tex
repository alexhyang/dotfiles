\documentclass[12pt]{exam}
\newcommand{\answer}{\textbf{\underline{Answer}}: }
\newcommand{\st}{\text{ s.t. }}

% Math 220 LaTeX notes

%% Proof Language

%   suppose, assume, let,
%   hence, so, then, since, by noting,
%   This means ..
%   We then argue by cases
%   In this case ...
%   It follows that ...
%   It suffices to show that ..
%   We have ...
%   We can ...
%   We see/conclude ...
%   Which yields/implies...

%   We use proof by cases. First, let ..

%   We prove this statement using contrapositive. Assume .., then we want to
%     show that .. Therefore in all cases, we see that.. Hence, the result
%     follows

%   We proceed by induction
%   We use induction (to prove the result)

\usepackage{amsmath,amsthm,amsfonts,amssymb}

%% Templates

\begin{proof}
  % define variable
  Let $x,y \in \mathbb{Z}$.

  % equivalence
  \(a^2+1
    \equiv 2\mod 4  %2  mod 4%
    \equiv 2\pmod 4 %2  (mod 4)%
    \equiv 2\bmod 4 %2 mod 4%

  % equations with text inside
  \(\exists k \in \mathbb{Z}\ \text{s.t.}\ n=2k\)
  \(3\ \text{s.t.}\ (continue here)\)

  % Induction
  $\bullet$ Base case: when \(n=__\), the statement is true since $\cdots$
  $\bullet$ Inductive step: Assume $\cdots$

\end{proof}
